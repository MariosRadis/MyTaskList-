@model Practice_1._1.Models.Task
@{
    ViewBag.Title = "CreateTask";
    Layout = "~/Views/Shared/_MyLayout.cshtml";
}

<div class="container-fluid">
    <div class="row col-md-3">
        <h1>CreateTask!!</h1>
        <form action="@Url.Action("CreateTask","Home")" method="post">
            <div class="form-group">
                <label for="Title">Title</label>
                <input class="form-control" name="Title" id="Title" placeholder="Task title" />
                @Html.ValidationMessageFor(i => i.Title, null, new { @class = "text-danger" })
            </div>
            <div class="form-group">
                <label for="Description">Description</label>
                <input class="form-control" name="Description" id="Description" placeholder="Description" />
                @Html.ValidationMessageFor(i => i.Description, null, new { @class = "text-danger" })
            </div>
            <div class="form-group">
                <label for="DueDate">DueDate</label>
                <input class="form-control" type="date" name="DueDate" id="DueDate" placeholder="DueDate" />
                @Html.ValidationMessageFor(i => i.DueDate, null, new { @class = "text-danger" })
            </div>
            <button type="submit" class="btn btn-primary">Create</button>
            <button type="button" class="btn btn-primary" onclick="@("window.location.href='"+Url.Action("AllTasks","Home")+"'")">Cancel</button>
        </form>
    </div>
</div>

